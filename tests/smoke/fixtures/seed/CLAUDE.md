# greet — CLAUDE.md

A one-module toy project: `lib/greet.mjs` builds the greeting, `check.mjs` is the project's
verification and must exit 0 (`node check.mjs`). Keep commits atomic, one concern each.
