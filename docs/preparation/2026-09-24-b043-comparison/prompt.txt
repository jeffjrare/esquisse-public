/esq:plan B-043 — Expose stale roadmap/epic state beside live ledger facts without adding a second writer.

This is a bounded, read-only preparation evaluation against this actual repository. User override: do not write files, create branches, change ledgers, commit, build, launch agents, or implement B-043. Instead return the concrete plan proposal in your final response, at most 1200 words, in French. Use the real backlog detail and code, not an invented application or assumptions passed off as facts. Retain existing constraints: deterministic structure in CLI, judgment in model, no second ledger writer, no mandatory refresh or new ceremony, minimal proportionate verification. Read only what informs this target. You may use Read, Grep and Glob; no command execution is needed for this read-only variant. Do not ask for implementation choices you can decide from the repository. Name any actual missing authority. Include the useful outcome, proposed CLI/user interaction, alternatives and architecture tradeoff, cost, one usable delivery slice and verification of the promised result. Cite real paths. The template's committing and mutation steps are explicitly out of scope for this evaluation.
