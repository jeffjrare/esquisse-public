{"phase":1,"status":"completed","date":"2026-09-25","planCommittedAt":"de5ec584",
"commits":["be47636e","e26bc900","41f7f3de","66b5ccdc","6a32f30a","1f8ddfbb"],
"whatBuilt":"On `/` and `/en/` the household section now sits between « Une prévision… » and « Tout ce qui est noté se relit ». Its column 1 carries a new `fragments/SpacingFragment.astro` tile: the app's amber spacing notice, word for word, over a neutral « Enregistrer quand même ». The hero subhead now says several people log together, the step-2 drawer draws the chips « 0,5 · 1 · 2 » with « 1 » pressed, and the second looking-back point names filtering by button and by answer. The height budget is 5 450 px, recorded as D-le-budget-de-hauteur-passe-a-5-450 and restated in every place that states it.",
"verification":[
"(auto) pnpm --filter landing build — astro check 0 errors, 9 pages built, check-csp passes (run inside `pnpm -r build` and again by the audit)",
"(auto) pnpm --filter landing test — 9 files, 134 tests pass",
"(auto) node landing/scripts/audit.mjs — exit 0: every text node clears AA in light and dark; header rows centred; / 5421 px and /en/ 5340 px, under 5450; first « ~ » ends at 229 px (above the fold) at 1280x800; horizontal overflow no on every reading. The step-2 phone fill went from 76.2 % to 84.4 %, so it does not overflow.",
"(auto) grep of the five sections in both index files — Steps, Forecasting, Features, LookingBack, Discover, in that order, on both",
"(auto) grep 5 100|5100 over the four places that state the limit — no match (exit 1)",
"(auto) grep of ASCII elisions in fr.json — no match (exit 1)",
"(auto) pnpm -r build — exit 0, all four packages (wall-clock shared with pnpm test: 5 min 25 s together)",
"(auto) pnpm test — exit 0: locale parity, legal and mark gates, backend 69 suites / 2085 tests, frontend 123 files / 2330 tests, landing 134 tests, layout probe",
"(manual) [/ 1280×800 light] observed with Playwright driving the system Chrome over `landing/dist` served on 127.0.0.1:4399: the subhead reads « … à plusieurs, chacun note d’un geste, et tout le monde voit ce que les autres ont fait … »; the h1 is unchanged; « ~ dans 5 h » ends at 229 px; the step-2 phone shows 0,5 · 1 · 2 under Quantité with « 1 » pressed (border-primary); the h2 order is … Une prévision → Toute la maisonnée → Tout ce qui est noté se relit → Ce que l’app fait aussi, with no household section after the bento; the amber notice text matches, the neutral « Enregistrer quand même » sits in column 1 and the switcher in column 3; the second point names « seulement les biberons en poudre ». Screenshots read. Console clean — PASS",
"(manual) [/en/ 1280×800 dark] same instrument: the notice reads \"Camille logged “Dose” 2 h ago — at least 6 h between two entries.\" over \"Save anyway\". Both fragments are legible on the dark palette in the screenshot, and the audit's AA covers them in dark — PASS",
"(manual) [/ and /en/ 360×800, light and dark] same instrument, four readings: the household stacks heading → lead → spacing tile → points → switcher tile (tops increase in that order, e.g. FR 7034/7118/7346/7610/7918); the notice is not clipped (scrollWidth/Height within the box); no horizontal overflow; the FR dark screenshot was read — PASS"],
"verified":{"at":"1f8ddfbb0276e25b88474f57546370b98578705f","commands":["pnpm --filter landing build","pnpm --filter landing test","node landing/scripts/audit.mjs","grep -n \"^  <\\(Steps\\|Forecasting\\|Features\\|LookingBack\\|Discover\\)\" landing/src/pages/index.astro landing/src/pages/en/index.astro","grep -rn \"5 100\\|5100\" landing/scripts/audit.mjs landing/DESIGN.md docs/ARCHITECTURE.md .claude/skills/landing-and-deploy/SKILL.md","grep -n \"[[:alpha:]]'[[:alpha:]]\" landing/src/copy/fr.json","pnpm -r build","pnpm test"]},
"surprises":"- **The spacing tile cost 286 px, over the plan's ~150 px trigger, and its caption stays.** I measured each commit on its own at 1280×800: moving the section 0 px, the spacing tile +286 px, the subhead 0 px, the chips 0 px, the filter point +45 px in FR and 0 in EN. Folding the caption into the label, which is the plan's named remedy, would save only 76 px (5345 / 5264) and would remove the one visible sentence that says the rule is between members and never blocks. The brief ruled « relever le plafond, sans retrait », so the limit went to 5 450 (29 px of margin). The reasoning is recorded in D-le-budget-de-hauteur-passe-a-5-450.\n- `features.household.spacing` gained an `emoji` key beyond the plan's { caption; label; title; notice; action }, so that 💊 is copy like every other emoji on the page, not a mark hard-coded into the fragment.\n- D-la-maisonnee-suit-les-previsions and D-l-ecart-minimum-prouve-la-maisonnee were already in DECISIONS.md from planning, so Task 1.6 added only the budget entry.\n- The `(manual)` steps name run-tamialog's « standalone landing window ». An `--app` window runs the landing's installed-icon handoff (`location.replace` to the app), so it would not have shown the page. I drove an ordinary Playwright context over `dist/` instead, at the plan's viewports and colour schemes.\n- In FR at 1280 the notice breaks between « 2 » and « h » (« il y a 2 / h »). The strings are the app's, with plain spaces, and nothing is clipped.\n- `/` at 390×844 has its first « ~ » below the fold (889 px). The same reading was already true on `de5ec584`, so this plan did not cause it. The plan's fold criterion is 1280×800.",
"backlogCandidates":"None.",
"forNextPhase":"Last phase — nothing follows. For review: to reproduce the walk, serve `landing/dist` on a throwaway port (not 4321) and drive Playwright (`node .claude/skills/run-tamialog/scripts/ensure-driver.mjs`, system Chrome) in a normal context, not `--app`. `node landing/scripts/audit.mjs` builds and serves on its own. `docs/SPEC.md` still has no section for the spacing rule or the recurring chips; `/esq:spec` would close that gap, and `SpacingFragment`'s and `QuickLogMock`'s headers should then cite it."}
