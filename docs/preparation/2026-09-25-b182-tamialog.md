# B-182 — Séparer la préparation du contrat de plan

## Changement retenu

Base locale : `fd77d23` (plugin 0.3.49). Seuls
`plugin/skills/plan/SKILL.md` et sa nouvelle référence
`references/record-contract.md` changent dans le produit.

La préparation garde l'investigation, les choix d'approche et de phases, l'UX,
l'autorité, le dimensionnement et la composition des preuves. Le gabarit et les
formalités de fichier, branches, décisions et lignes Planned sont chargés lorsque
le résultat doit être écrit et enregistré. Une proposition demandée en réponse
seule termine avant ce chargement. La découverte de l'outil de question arrive
avec une décision qui nécessite l'utilisateur, plus systématiquement en preflight.

Les instructions de branche avaient deux versions dans l'entrée : leur procédure
est désormais réunie dans la référence d'enregistrement. Les rappels redondants
de chargement UI au gabarit et au dimensionnement sont retirés ; les déclencheurs
UI/manuels restent dans l'entrée, y compris lorsque l'investigation élargit la
portée. La référence UI de B-181 reste **inchangée**. Aucun rappel spécifique au
champ « 1 comprimé » ou au cas Tamialog n'est ajouté.

Le [diff complet](2026-09-25-b182-tamialog/change.json) et les
[empreintes](2026-09-25-b182-tamialog/source-metrics.json) décrivent le déplacement.
L'entrée passe de 4 874 à 3 365 mots ; la référence en contient 1 432, soit seulement
77 mots retirés au total. Ces nombres décrivent la source, **pas un bénéfice produit
ni une économie observée**. Un plan enregistré paie encore la lecture du contrat.

## Relecture des garanties

- Les approches viables, leurs bénéfices/coûts/limites et les tranches utiles sont
  préparées avant le formatage. La sécurité, les états UI, la fidélité au mandat,
  les contre-exemples et les preuves proportionnées restent dans ce chemin.
- L'autorité utilisateur, `esq standards`, la recherche bornée et la reprise des
  recherches du brief sont conservés. L'enregistrement significatif charge encore
  explicitement `references/decisions.md`, avec son format et son Fondement intact.
- Le correctif charge toujours `references/corrective-input.md` avant investigation :
  `exhausted` n'atteint aucune mutation. Le contrat conserve `esq branch resolve`,
  reuse/new, l'origine de la souche, le refus d'une branche cible ambiguë, les refs
  citées et la préservation du travail sale.
- Le gabarit conserve les sections, `**Branch:**`, `**Origin:**`, `**Epic:**`, les
  phases et le marqueur réservé de `## Execution log`. `Done looks like` reste
  repris intact. Les writers ne changent pas ; statuts via CLI, `Planned by`,
  retrait du seul brief correctif consommé et commit de bookkeeping unique restent
  ensemble. Aucun log d'exécution n'est rempli par plan.
- La préparation garde les règles `(auto)`/`(manual)`, les commandes résolues par
  lecture, la déduplication des preuves, les propriétés distinctes et le chargement
  conditionnel de `reads-declaration.md`. Le contrat écrit est relu avant son commit
  et `esq validate` reste obligatoire. Les sorties build/autopilot restent intactes.
- Le README décrit toujours un plan vérifiable committé sur sa branche. Aucun
  nouveau mode public, writer, schéma, rôle, test lexical ou cérémonie d'approbation.

## Essai borné et référence réutilisée

Référence : la [proposition après B-181](2026-09-25-b181-tamialog/after-proposal.md),
**jamais relancée**. Son jugement et ses limites restent ceux de
[B-181](2026-09-25-b181-tamialog.md).

L'archive B-181 existante est vérifiée contre les SHA-256 des **437 sources** et des
44 fichiers du plugin, avec ses deux corrections conservées. Copie isolée sans Git,
plan final/décisions ultérieures/captures toujours exclus. Seuls les deux fichiers
ci-dessus sont remplacés/ajoutés : **482 fichiers**, identiques avant/après le
lancement. [Inventaire et écarts](2026-09-25-b182-tamialog/inputs.json).
`events-tracker` n'est ni modifié ni relancé ; sa copie conservée suffit.

Même [prompt](2026-09-25-b182-tamialog/prompt.txt), mêmes
[critères préalables](2026-09-25-b181-tamialog/judgment.md), Claude Code 2.1.282,
Opus/medium, outils Read/Grep/Glob/Skill, `dontAsk`, `--restricted`, hooks/MCP/mémoire
et persistance désactivés ; **3 USD / 180 secondes** annoncés avant lancement.
Le plugin de l'archive demeure 0.3.48, comme la référence ; pas d'installation.

Un premier lancement s'arrête en **0,33 s**, avant outil ou appel modèle observable :
la copie temporaire d'authentification B-181 avait été supprimée. La réponse native
est « Not logged in », avec **0 USD** rapporté. Cet
[échec de démarrage](2026-09-25-b182-tamialog/startup-metrics.json) est conservé.
Une nouvelle copie temporaire des identifiants locaux valides est alors utilisée,
puis supprimée ; aucun fichier sous `~/.claude/` n'est écrit.

L'essai s'arrête à **180,03 s**, processus tué à la borne de 180 s. Il ne retourne
**aucune proposition, aucun message assistant, aucun appel outil et aucun résultat
final**. La trace contient dix reprises API, toutes `error: unknown` :
[événements](2026-09-25-b182-tamialog/runtime-events.json),
[métriques](2026-09-25-b182-tamialog/candidate-metrics.json).
L'environnement a un réseau restreint ; la cause exacte des reprises n'est pas
établie. Le coût final est **inconnu**, pas zéro : seul le plafond configuré de
3 USD est établi. Aucun autre essai, aucune extension de délai ou de budget.

## Comparaison possible et limites

| Dimension | Référence conservée B-181 | B-182 |
| --- | --- | --- |
| Fidélité au brief | Ordre, trois nouveautés et sélecteur conservés ; champ figé maintenu à tort ; Git inventé | Non observable, aucune proposition |
| Qualité des choix | Rendu du tiroir consulté, disposition desktop/md/mobile et copie FR/EN ; pas de rendu livré | Non observable |
| Exécution | Une phase, no-build ; séquence mesure/seuil/preuve encore imprécise ; observation 360 px peu étayée | Non observable |
| Longueur finale | 1 313 mots, échec du maximum 900 | Pas de texte à compter |
| Outils / caractères retournés | 41 / 120 541 | 0 / 0 : échec d'accès, pas une économie |
| Temps | 162,93 s | 180,03 s, timeout |
| Coût USD rapporté | 1,1363492 | Inconnu ; plafond configuré 3 |

La séparation est justifiée structurellement : les formalités de persistance ne
sont plus nécessaires pour préparer une proposition. **Aucun bénéfice de fidélité,
de concision, de qualité ou de coût n'est observé sur une sortie candidate.** Aucun
défaut de la référence n'est déclaré corrigé par cet essai. B-182 reste **Open** ;
B-181 et B-169/C restent également ouverts. Ni B-070 ni B-005 ne sont repris.

Le coût de reprise est borné avant dépense ; aucun résultat acquis n'est racheté.
Le jugement humain demeure nécessaire sur la proposition réelle et son rendu :
l'extraction du gabarit ne les remplace pas. Pas de campagne ajoutée ni de nouveau
critère automatique de présence de prose.

## Vérification locale

Un seul `env -u ESQ_TELEMETRY ./scripts/audit.sh`, borne **180 s**, termine en
**41,82 s**, exit 1 : **5/7 PASS**, dont structure, validation native du plugin,
formats parsés, absence de push et accord des versions. Le lanceur borné et les
suites produit échouent au niveau de fichiers enfants ; le résumé produit est
3 pass, 12 fail, 1 cancelled. Ce ne sont pas des assertions produit exécutées avec
succès. [Audit initial](2026-09-25-b182-tamialog/audit.txt),
[environnement et empreintes](2026-09-25-b182-tamialog/audit-metrics.json).

La même forme d'échec est documentée dans B-013. Seuls les deux contrôles en échec
sont repris hors sandbox, sous une borne commune de **160 s** ; les cinq contrôles
acquis ne sont pas rejoués. **Les deux passent** : lanceur en **9,04 s**, suites
produit en **15,30 s**. [Commandes, résultats et empreintes](2026-09-25-b182-tamialog/audit-recovery.json),
[sortie produit](2026-09-25-b182-tamialog/product-recovery.txt).
Cela établit une dépendance à l'environnement, sans identifier le mécanisme exact.
C'est cinq succès initiaux plus deux reprises réussies, **pas un audit initial 7/7**.
Aucun test, script ou code runtime n'est modifié ; aucune suite supplémentaire.

Les seules écritures après vérification sont ces preuves et les suivis de registres.
Aucun statut n'est fermé, aucune décision produit n'est enregistrée sur une sortie
absente. Les mesures de préparation restent dans leur dossier habituel ; aucune
ligne de télémétrie ou preuve de routage n'est activée dans `docs/EVIDENCE.md`.
Aucun push, publication ou installation.

Validation finale des registres : `valid: true`, aucun finding ; `git diff --check`
passe. [Résultat](2026-09-25-b182-tamialog/ledger-validation.json). Les empreintes
des sources de l’essai correspondent à l’audit et aux deux reprises. Le contrôle
du diff indexé a ensuite signalé un espace final hérité après `Tasks` et une ligne
vide finale dans la nouvelle référence : nettoyage purement typographique, sans
rejouer l’audit. Le diff est conservé en JSON pour préserver exactement ses espaces
de contexte. [Écart final d’empreinte](2026-09-25-b182-tamialog/final-formatting.json).
