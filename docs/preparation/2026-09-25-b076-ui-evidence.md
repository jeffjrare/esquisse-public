# B-076 — Reopen the observed UI state from the existing log

## Before / after

- Before: the real landing plan said “Screenshots read” and recorded six route/viewport/theme readings, but linked no image or rendered-output artifact. A reviewer could recover conclusions and a reproduction recipe, not open the observed states from the log.
- After: build preserves useful existing captures and records state-specific links in `verification` or `manual[].observed`; check/review open either completed-log form and distinguish missing evidence from a visibly wrong result. Human confirmation needs no additional image. No CLI, schema, service or mandatory capture matrix was added.

## Read-only source and recovered evidence

Source: `/home/jeffgirard/projects/events-tracker/docs/plans/2026-09-24-vitrine-fonctions-recentes-multi-membre.md`, Phase 1 completed 2026-09-25, recorded implementation `1f8ddfbb0276e25b88474f57546370b98578705f`. Source SHA-256 at inspection: `e7eaf9757a173d25f3c8751d0bce8b2fbf668b10fe69bcd94cf2913f22898890`.

The source log has no image path. A bounded search in the project's docs and `/tmp` found ten PNGs and `walk.json` at `/tmp/claude-1000/-home-jeffgirard-projects-events-tracker/88a0054e-5668-47ce-9892-535ce7fe70bf/scratchpad/shots/`. The sibling [original entry](2026-09-25-b076-ui-evidence/original-entry.json) has exactly the source plan's manual lines and the same `verified.at`. Its image names, visible strings and [walk readings](2026-09-25-b076-ui-evidence/walk.json) agree with those states. This establishes a recovered association, not a contemporaneous cryptographic binding of each PNG to that commit. No fresh UI run or new capture was made; events-tracker was not modified.

All ten images were opened during this change. They depict the static public landing's mock data. Copies retain the original bytes (902,115 bytes for the ten PNGs, walk and original entry together). The log below records today's reinspection of those recovered artifacts, not a new execution or certification of the external plan. Paths are relative to this document and survive moving the checkout.

## Review boundaries

| Case | Reading and disposition |
|---|---|
| Correct state, readable reference | Open and inspect it; reuse without a new capture. |
| Missing/unreadable link | Missing evidence; recover the existing artifact first. Do not infer a visual failure or erase an actual human observation. |
| FR light image offered for EN dark | Wrong-state proof, even if the image itself looks correct. The EN dark link below provides the actual state. |
| Mobile crop ends before the switcher | It proves notice wrapping and the visible stack only. `walk.json` separately records full block positions and overflow; it is not a screenshot of the off-screen switcher. |
| Changed implementation since observation | Assess dependencies with existing history; the old picture remains historical evidence, not proof of the changed state. |
| Human explicitly confirmed a named state | Retain who/when/what was confirmed without requiring an image. No human confirmed the actual landing in this session. |
| A result is visibly wrong | Report the observed defect; a missing file alone cannot establish it. No new external-app defect is claimed here. |

## Verification and cost

The bound was one local CLI exercise of the two existing recording paths and a fresh-reader portability check, followed by one product audit (suite bound 120 s, outer bound 240 s). No separate node suite, app build, live model journey, subagent, screenshot generation or release pass. This verifies artifact retrieval and deterministic logging, not future model compliance with prose instructions.

- `appendLog` produced the six real reinspection rows below using existing `verification` strings; no added payload fields.
- `resolveBlock` in a throwaway Git repository preserved a link in `manual[].observed`, completed the pause and returned `reran: []`. A second, explicitly synthetic human-confirmation input without an image also survived. This fixture is not a claim that a human confirmed the landing.
- A separate Node reader in a local clone opened 17 references across the two log forms, resolving paths from each log: 12 distinct artifacts, including all ten PNGs, identical byte-for-byte to the preserved sources. It distinguished six route/width/theme tuples in `walk.json`; notice clipping and horizontal overflow were false, the second chip selected, and mobile block tops in the stated order. These are recovered DOM readings, not rerun browser measurements.
- The first fixture attempt hit sandbox `spawnSync git EPERM` before logging the pause. The authorized local retry completed in 0.17 s; no product check had run twice.
- The fresh clone's EN 1280 dark and FR 360 dark links were then reopened with the image viewer. They show the named language/theme/width, amber notice and neutral action; the FR mobile switcher remains outside the image. This was another read of the stored evidence, not a new app observation or an independent model review.
- `timeout --kill-after=5 240 ./scripts/audit.sh` — exit 0, **7/7 product checks**, once; [audit output](2026-09-25-b076-ui-evidence/audit.txt). Approximately 28 s from output-file creation to final output. No implementation change followed this pass; only result/ledger bookkeeping. Plugin validation was included in the audit; no live model run was purchased.
- After the B-076 status/resolution and scoped roadmap state update, `plugin/bin/esq validate` returned `valid: true`, no findings; `git diff --check` was clean. The external plan's SHA-256 still matched the initial read. No events-tracker file was written, and no push, installation or publication was performed.

## Phases

### Phase 1 — Preserve and reinspect the recovered states

Open each recovered image, record what it actually shows, and keep the references reachable from this log. Completion below means this preservation/reinspection only.

## Execution log
<!-- Appended through esq plan append-log; this is an evidence specimen, not the external plan. -->

### Phase 1 — completed 2026-09-25

**Plan committed at:** unversioned

**Commits:** none

**What got built:** Preserved existing landing evidence in esquisse; no external implementation changed.

**Verification:**
- (manual) [/ 1280×800 light; static landing, scroll hero → step 2 → household] Reinspected 2026-09-25 with image viewer; original instrument: Playwright/system Chrome over dist; original log attributes implementation 1f8ddfbb0276e25b88474f57546370b98578705f (recovered association, not an embedded image commit). Expected and seen: shared-use subhead and ~5 h above the fold in [hero](2026-09-25-b076-ui-evidence/hero-fr-1280-light.png); three quantity chips with 1 selected in [step 2 crop](2026-09-25-b076-ui-evidence/step2-fr-light.png); amber notice, neutral save-anyway action in column 1, switcher in column 3 in [household](2026-09-25-b076-ui-evidence/household-fr-1280-light.png). PASS for these visible properties; original h1 equality and the whole page order are not established by these images alone.
- (manual) [/en/ 1280×800 dark; static landing, scroll hero → step 2 → household] Reinspected 2026-09-25 with image viewer; original instrument: Playwright/system Chrome over dist; original log attributes implementation 1f8ddfbb0276e25b88474f57546370b98578705f (recovered association, not an embedded image commit). Expected and seen: shared-use subhead and ~5 h above the fold in [hero](2026-09-25-b076-ui-evidence/hero-en-1280-dark.png); three quantity chips with 1 selected in [step 2 crop](2026-09-25-b076-ui-evidence/step2-en-dark.png); amber notice, neutral save-anyway action in column 1, switcher in column 3 in [household](2026-09-25-b076-ui-evidence/household-en-1280-dark.png). PASS for these visible properties; original h1 equality and the whole page order are not established by these images alone.
- (manual) [/ 360×800 light; static landing, scroll to household] Reinspected 2026-09-25 with image viewer; original instrument and implementation attribution as above. Expected and seen in [household viewport](2026-09-25-b076-ui-evidence/household-fr-360-light.png): heading → lead → amber spacing tile → beginning of points, notice fully visible and neutral Enregistrer quand même action. PASS for the visible crop. Switcher is below the crop; [original DOM readings](2026-09-25-b076-ui-evidence/walk.json) identifies this route/width/scheme and records its position after the points and hOverflow=false. DOM geometry does not establish off-screen visual legibility.
- (manual) [/ 360×800 dark; static landing, scroll to household] Reinspected 2026-09-25 with image viewer; original instrument and implementation attribution as above. Expected and seen in [household viewport](2026-09-25-b076-ui-evidence/household-fr-360-dark.png): heading → lead → amber spacing tile → beginning of points, notice fully visible and neutral Enregistrer quand même action. PASS for the visible crop. Switcher is below the crop; [original DOM readings](2026-09-25-b076-ui-evidence/walk.json) identifies this route/width/scheme and records its position after the points and hOverflow=false. DOM geometry does not establish off-screen visual legibility.
- (manual) [/en/ 360×800 light; static landing, scroll to household] Reinspected 2026-09-25 with image viewer; original instrument and implementation attribution as above. Expected and seen in [household viewport](2026-09-25-b076-ui-evidence/household-en-360-light.png): heading → lead → amber spacing tile → beginning of points, notice fully visible and neutral Save anyway action. PASS for the visible crop. Switcher is below the crop; [original DOM readings](2026-09-25-b076-ui-evidence/walk.json) identifies this route/width/scheme and records its position after the points and hOverflow=false. DOM geometry does not establish off-screen visual legibility.
- (manual) [/en/ 360×800 dark; static landing, scroll to household] Reinspected 2026-09-25 with image viewer; original instrument and implementation attribution as above. Expected and seen in [household viewport](2026-09-25-b076-ui-evidence/household-en-360-dark.png): heading → lead → amber spacing tile → beginning of points, notice fully visible and neutral Save anyway action. PASS for the visible crop. Switcher is below the crop; [original DOM readings](2026-09-25-b076-ui-evidence/walk.json) identifies this route/width/scheme and records its position after the points and hOverflow=false. DOM geometry does not establish off-screen visual legibility.

**Surprises / decisions made during execution:** The source log had no durable image links. Mobile screenshots stop above the switcher; coverage is explicitly limited.

**For Phase 2:** No next phase. Reopen the relative artifact links above; retain the original entry as historical provenance.
