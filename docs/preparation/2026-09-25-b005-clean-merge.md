# B-005 — A clean Git merge of one ancestor-present item

Date: 2026-09-25. Starting commit: `119ba3b`. Maintainer regression,
local only; no installation, push or publication.

## Scope and reused evidence

The B-005 detail asks for a combined marker-free, ancestor-present path.
The existing `scan classifies a duplicate present at the base` test injects
duplicates after begin. [B-003](2026-09-24-b003-retained-branch.md) proves
retained-branch ID reservation; [B-184](2026-09-24-b184-merge-ranks.md) proves
rank reconciliation for distinct IDs. Neither supplies this combined proof.
Their historical results are reused, not rerun separately or rewritten.

The current `plugin/skills/worktree/SKILL.md`, Merge steps 2–4, explicitly
requires scanning even clean merges and collapsing present-at-base copies
outside conflict hunks in the attended caller, using returned cell verdicts.
`applyDerivedRows` in `plugin/lib/cli.mjs` processes conflicted table hunks only.
Thus the historical step numbering changed, but the requested outcome remains
applicable. No acceptance was weakened to require automatic sealing of duplicates.

## Regression and observed outcome

Added one test to `tests/cli/merge.test.mjs`, using its existing real-Git fixture
and engine, with no new framework or product code:

1. The ancestor contains B-001 between ten stable anchor rows. Main moves it
   to the top and changes Status from Open to Planned. The source moves it to
   the bottom and changes Pri from med to hi. This is a valid unranked ledger;
   physical row position does not encode a stored Rank change.
2. Validate the ancestor and each branch independently, each with exactly one
   B-001. `mergeBegin` runs Git's real merge. Both sides deleted the old position;
   Git accepts both distant insertions. Assert clean=true, no unmerged index
   entries or markers, live MERGE_HEAD, and the exact two-copy result. No file
   is written between begin and observing/scanning the duplicate.
3. Assert exactly one duplicate classification: B-001, row, count 2,
   present-at-base. The reported three-way cells derive hi and Planned through
   `one-side-unchanged`. Scan itself leaves the bytes unchanged.
4. Exercise the documented attended action: retain one copy and copy only the
   returned values into its cells. No precedence rule is reimplemented. Rescan
   all four duplicate predicates clean, validate, and seal.
5. Assert the entire final ledger: one B-001 with hi + Planned + shared, all
   anchors unchanged, valid ledger, clean tree, no held merge, original source
   commit unchanged and both expected merge parents.

The existing absent-at-base collision, divergent free-text/Rank, marker refusal
and unattended-abort tests remain unchanged and passed in the same product audit.
No silent loss, ID reassignment or new arbitrary-choice rule was introduced.

## Verification, cost and limits

One `timeout 180s ./scripts/audit.sh`: exit 0, **7/7 PASS**, including product
suites and native package validation. [Captured output](2026-09-25-b005-audit.txt).
No standalone Node suite before or after it. The audit ran outside the sandbox
using the previously established B-003/B-184 child-process restriction, rather
than buying another failing sandbox attempt. No environment failure occurred
in this run, and none was treated as a product defect.

No product defect reproduced: only the missing regression and its evidence are
delivered. The test deterministically performs the prescribed caller edit; it
does not measure a live model's compliance or promise unattended reconciliation
of clean duplicates. Existing synthetic decision/detail-section coverage is
retained; this combined real-Git fixture covers a backlog row.

No paid model trial, subagent, network query, new dependency, runtime work or
registry. Added cost is one temporary local Git scenario in the existing suite;
all successful historical evidence is reused. Only bookkeeping follows the PASS.
B-005 alone is closed through `esq backlog set-status`; the concerned roadmap
entry is refreshed through its writer's rules. B-070 and all other statuses
remain untouched. No retrospective plan or execution log is created for inline work.
After bookkeeping, `esq validate` returned `valid: true` with no findings and
`git diff --check` passed. The product PASS is reused without another audit.
